# Tasks: auth-cookie-path

## Completed

- [x] **T-1** — Change `path: '/auth'` to `path: '/'` in `refreshCookieOptions()` (`cookie.helper.ts`)
- [x] **T-2** — Change `path: '/auth'` to `path: '/'` in `clearRefreshCookie()` (`cookie.helper.ts`)
