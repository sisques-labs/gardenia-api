import './care-schedule-registered-enums.graphql';

describe('care-schedule-registered-enums.graphql', () => {
  it('registers the GraphQL enums on import without throwing', () => {
    expect(true).toBe(true);
  });
});
